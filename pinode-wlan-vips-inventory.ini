# Deliberately separate from variables/cli/hosts.ini - see
# pinode-wlan-vips.yml's header comment for why pinode-01 isn't (and
# shouldn't be) added to that shared inventory.
[pinode01]
pinode-01 ansible_host=192.168.1.11 ansible_user=gorttman ansible_ssh_private_key_file=~/.ssh/pinode_cluster_ed25519
