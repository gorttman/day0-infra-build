---
 - name: Template out  the bootstrap app 
   include_tasks: template_bootstrap.yml

 - name: Prepare Argo for bootstrap
   ansible.builtin.include_tasks:
     file: argo_bootstrap_prep.yml

 - name: Apply secrets
   ansible.builtin.include_tasks:
    file: manage_repo_secrets.yml

 - name: Restore sealed-secrets controller key backup (if any) before ArgoCD deploys a fresh controller
   ansible.builtin.include_tasks:
    file: restore_sealed_secrets_key.yml

 - name: Apply ArgoCD bootstrap
   include_tasks: apply_bootstrap.yml

 - name: Start/Expose Kubernetes Dashboard UI
   include_tasks: start_k3s_ui.yml 
