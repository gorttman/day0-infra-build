# /etc/exports - NFS exports configuration for diskless Raspberry Pi cluster
# Generated by Ansible - setup-nfs-server role

# Base root filesystem - shared read-only by all nodes
{{ NFS_EXPORT_PATH }}/base {{ nfs_allowed_network }}({{ nfs_base_export_options }})

# Per-node state and k3s data directories - each node gets its own writable directories
# These use the %H hostname expansion on the client side, but we export the parent directory
{{ NFS_EXPORT_PATH }}/nodes {{ nfs_allowed_network }}({{ nfs_node_export_options }})
