---
- name: Reboot required
  reboot:
    msg: "Reboot required to enable memory cgroups"
    connect_timeout: 30
    reboot_timeout: 300
  become: true

- name: Restart timesyncd
  ansible.builtin.systemd:
    name: systemd-timesyncd
    state: restarted
  become: true
